# Redefine properties
# Note that you do not need to prefix the property here
sonar.projectName=griffin-models

sonar.sources=src/main/scala
sonar.scala.binaries=target/classes
sonar.sourceEncoding=UTF-8
sonar.language=scala